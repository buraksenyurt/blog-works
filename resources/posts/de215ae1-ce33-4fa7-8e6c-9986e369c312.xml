<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<post>
  <title>Rust ile Kodlama İdmanları - Orta Seviye</title>
  <description>Bir önceki yazımızda Rust programlama dili için başlangıç seviyesinde değerlendirebileceğimiz örnek kodlara değinmiştik. Bu yazımızda ise orta seviye konulara ait örneklere yer vermeye gayret edeceğiz. Composition'dan kapsamlı test senaryolarına, lazy iterator kullanımından generic türlerde kısıtlamaları kullanmaya kadar farklı konu başlıklarında kısa ve hatırlatıcı bilgileri örnek kodlar ile ele alacağız.</description>
  <content>&lt;p&gt;&lt;img style="float: right;" src="/image.axd?picture=/2025/Kasim/ferris_mini_11.png" alt="" /&gt;Şunu fark ettim ki, hangi programlama dili olursa olsun bilgilerimizi taze tutmanın yollarından birisi &amp;ouml;ğrenilenleri d&amp;uuml;zenli olarak not haline getirmek ve yazarak kayıt altına almak. Elbette tek yol bu değil. Mutlaka her g&amp;uuml;n bir par&amp;ccedil;a da olsa kod yazmak, belki bir proje &amp;uuml;zerinden ilerlemek&amp;nbsp;&lt;em&gt;(hi&amp;ccedil;bir yerde kullanılmayacak olsa bile)&lt;/em&gt;, onunla ilgili bir makale okumak veya bir video izlemek lazım. &amp;Uuml;nl&amp;uuml; d&amp;uuml;ş&amp;uuml;n&amp;uuml;r &lt;strong&gt;Johann Wolfgang von Goethe&lt;/strong&gt; ne demiş &lt;strong&gt;&lt;em&gt;"İnsan her g&amp;uuml;n bir par&amp;ccedil;a m&amp;uuml;zik dinlemeli, iyi bir şiir okumalı, g&amp;uuml;zel bir tablo g&amp;ouml;rmeli ve m&amp;uuml;mk&amp;uuml;nse birka&amp;ccedil; mantıklı c&amp;uuml;mle s&amp;ouml;ylemelidir."&amp;nbsp;&lt;/em&gt;&lt;/strong&gt;Yapay zekanın bilgisayar ve internet'ten sonra yeni bir devrim olma &amp;ccedil;abasıyla koştuğu şu d&amp;ouml;nemde iyi bir programcı olmak i&amp;ccedil;in daha &amp;ccedil;ok okuyalım, daha &amp;ccedil;ok pratik yapalım, daha &amp;ccedil;ok araştıralım, daha &amp;ccedil;ok dinleyelim derim. Elbette yapay zeka ara&amp;ccedil;larına sırtımızı da d&amp;ouml;nmeyelim. Nimetlerinden, verimliliğimizi artıracak &amp;ouml;l&amp;ccedil;&amp;uuml;de yararlanalım.&lt;/p&gt;
&lt;p&gt;Bir &amp;ouml;nceki yazımızda Rust programlama dili i&amp;ccedil;in başlangı&amp;ccedil; seviyesinde değerlendirebileceğimiz &amp;ouml;rnek kodlara değinmiştik. Bu yazımızda ise orta seviye konulara ait &amp;ouml;rneklere yer vermeye gayret edeceğiz. Composition'dan kapsamlı test senaryolarına, lazy iterator kullanımından generic t&amp;uuml;rlerde kısıtlamaları kullanmaya kadar farklı konu başlıklarında kısa ve hatırlatıcı bilgileri &amp;ouml;rnek kodlar ile ele alacağız. Dilerseniz vakit kaybetmeden ilk konumuzla başlayalım.&lt;/p&gt;
&lt;h2&gt;Composition Over Inheritance ile Daha Mod&amp;uuml;ler Tasarım&lt;/h2&gt;
&lt;p&gt;Rust nesne y&amp;ouml;nelimli programlama&lt;em&gt;(Object Oriented Programming)&lt;/em&gt; paradigmalarını tam olarak destekler mi desteklemez mi veya buna ihtiyacı var mıdır bilinmez ancak bir&amp;ccedil;ok tasarım kalıbı uygulanabilir. Hatta temel prensiplerden birisi olan&amp;nbsp;&lt;strong&gt;Composition over Inheritance&lt;/strong&gt;&amp;nbsp;olduk&amp;ccedil;a &amp;ouml;n plana &amp;ccedil;ıkar. &lt;strong&gt;Bevy&lt;/strong&gt; gibi &lt;strong&gt;ECS&lt;em&gt;(Entity Component System)&lt;/em&gt;&lt;/strong&gt; tabanlı oyun motorları bu prensibi temel alarak geliştirilmiştir. Bir nesnenin ihtiya&amp;ccedil; duyduğu &amp;ouml;zellik ve davranışların başka nesnelerden kalıtım yoluyla alınmasından ziyade bileşenler&lt;em&gt;(components)&lt;/em&gt; aracılığıyla alınması tercih edilir. Bu yaklaşım kodun daha esnek, yeniden kullanılabilir ve test edilebilir olmasını sağlar.&lt;/p&gt;
&lt;p&gt;Bir yazılım sistemindeki kullanıcıları temsil edecek bir yapı geliştirmeye &amp;ccedil;alıştığımızı d&amp;uuml;ş&amp;uuml;nelim. Kullanıcı ile ilgili t&amp;uuml;m bilgileri tek bir &lt;strong&gt;God Object&lt;/strong&gt; i&amp;ccedil;inde toplamak yerine, kullanıcıya ait farklı &amp;ouml;zellikleri ve davranışları ayrı bileşenler olarak tasarlayıp, kullanıcı yapısını ona bu bileşenleri ekleyerek oluşturmak daha esnek bir tasarım sağlar. Bu ama&amp;ccedil;la aşağıdaki &amp;ouml;rnek kod par&amp;ccedil;asını değerlendirebiliriz.&lt;/p&gt;
&lt;pre class="brush:cpp;auto-links:false;toolbar:false" contenteditable="false"&gt;fn main() {
    let personal_info = PersonalInfo::new("John".to_string(), "Doe".to_string(), 25);
    let contact_info = ContactInfo::new("john.doe@nowhere.com".to_string());
    let activity_status = ActivityStatus::new(true, 120120044543);
    let gaming_info = GamingInfo::new(7);

    let user = User::new(personal_info, contact_info, activity_status, gaming_info);

    println!("User: {}", user.get_full_name());
    println!("Email: {}", user.get_email());
    println!("Active: {}", user.is_active());
    println!("Level: {}", user.get_level());

    let mut mutable_user = user;
    mutable_user.set_active(true);
    mutable_user.level_up();

    println!("New level: {}", mutable_user.get_level());
}

// Bad Practice: God Object - T&amp;uuml;m bilgileri tek bir struct'ta toplamak
#[allow(dead_code)]
struct BadUser {
    first_name: String,
    last_name: String,
    age: u8,
    email: String,
    is_active: bool,
    last_activity_timestamp: u64,
    level: u8,
}

// Good Practice: Composition over Inheritance - Farklı sorumlulukları ayrı bileşenlerde tutmak
#[derive(Debug, Clone)]
struct PersonalInfo {
    first_name: String,
    last_name: String,
    age: u8,
}

impl PersonalInfo {
    fn new(first_name: String, last_name: String, age: u8) -&amp;gt; Self {
        Self {
            first_name,
            last_name,
            age,
        }
    }

    fn get_full_name(&amp;amp;self) -&amp;gt; String {
        format!("{} {}", self.first_name, self.last_name)
    }

    fn get_age(&amp;amp;self) -&amp;gt; u8 {
        self.age
    }
}

#[derive(Debug, Clone)]
struct ContactInfo {
    email: String,
}

impl ContactInfo {
    fn new(email: String) -&amp;gt; Self {
        Self { email }
    }

    fn get_email(&amp;amp;self) -&amp;gt; &amp;amp;str {
        &amp;amp;self.email
    }

    fn update_email(&amp;amp;mut self, new_email: String) {
        self.email = new_email;
    }
}

#[derive(Debug, Clone)]
struct ActivityStatus {
    is_active: bool,
    last_activity_timestamp: u64,
}

impl ActivityStatus {
    fn new(is_active: bool, last_activity_timestamp: u64) -&amp;gt; Self {
        Self {
            is_active,
            last_activity_timestamp,
        }
    }

    fn is_active(&amp;amp;self) -&amp;gt; bool {
        self.is_active
    }

    fn set_active(&amp;amp;mut self, active: bool) {
        self.is_active = active;
        if active {
            self.last_activity_timestamp = std::time::SystemTime::now()
                .duration_since(std::time::UNIX_EPOCH)
                .unwrap()
                .as_secs();
        }
    }

    fn get_last_activity(&amp;amp;self) -&amp;gt; u64 {
        self.last_activity_timestamp
    }
}

#[derive(Debug, Clone)]
struct GamingInfo {
    level: u8,
}

impl GamingInfo {
    fn new(level: u8) -&amp;gt; Self {
        Self { level }
    }

    fn get_level(&amp;amp;self) -&amp;gt; u8 {
        self.level
    }

    fn level_up(&amp;amp;mut self) {
        if self.level &amp;lt; u8::MAX {
            self.level += 1;
        }
    }

    fn set_level(&amp;amp;mut self, level: u8) {
        self.level = level;
    }
}

#[derive(Debug, Clone)]
struct User {
    personal_info: PersonalInfo,
    contact_info: ContactInfo,
    activity_status: ActivityStatus,
    gaming_info: GamingInfo,
}

#[allow(dead_code)]
impl User {
    fn new(
        personal_info: PersonalInfo,
        contact_info: ContactInfo,
        activity_status: ActivityStatus,
        gaming_info: GamingInfo,
    ) -&amp;gt; Self {
        Self {
            personal_info,
            contact_info,
            activity_status,
            gaming_info,
        }
    }

    fn get_full_name(&amp;amp;self) -&amp;gt; String {
        self.personal_info.get_full_name()
    }

    fn get_age(&amp;amp;self) -&amp;gt; u8 {
        self.personal_info.get_age()
    }

    fn get_email(&amp;amp;self) -&amp;gt; &amp;amp;str {
        self.contact_info.get_email()
    }

    fn update_email(&amp;amp;mut self, new_email: String) {
        self.contact_info.update_email(new_email);
    }

    fn is_active(&amp;amp;self) -&amp;gt; bool {
        self.activity_status.is_active()
    }

    fn set_active(&amp;amp;mut self, active: bool) {
        self.activity_status.set_active(active);
    }

    fn get_last_activity(&amp;amp;self) -&amp;gt; u64 {
        self.activity_status.get_last_activity()
    }

    fn get_level(&amp;amp;self) -&amp;gt; u8 {
        self.gaming_info.get_level()
    }

    fn level_up(&amp;amp;mut self) {
        self.gaming_info.level_up();
    }

    fn set_level(&amp;amp;mut self, level: u8) {
        self.gaming_info.set_level(level);
    }

    fn get_user_summary(&amp;amp;self) -&amp;gt; String {
        format!(
            "User: {} ({}), Email: {}, Active: {}, Level: {}",
            self.get_full_name(),
            self.get_age(),
            self.get_email(),
            self.is_active(),
            self.get_level()
        )
    }
}&lt;/pre&gt;
&lt;p&gt;Bu &amp;ouml;rnekte doğrudan bir kalıtım kullanımı s&amp;ouml;z konusu değildir ancak &lt;strong&gt;User&lt;/strong&gt; veri yapısının tasarımına dikkat edilmelidir. Personel, iletişim, aktivite ve oyun bilgileri ayrı birer veri yapısı olarak tasarlanmış ve &lt;strong&gt;User&lt;/strong&gt; isimli veri yapısında birer alan olarak kullanılmışlardır. &lt;strong&gt;Bad Practice&lt;/strong&gt; olarak tasarlanan &lt;strong&gt;BadUser&lt;/strong&gt; veri yapısına g&amp;ouml;re yeniden kullanılabilir bileşenler s&amp;ouml;z konusudur. Bir başka deyişle farklı veri yapılarında da kullanılabilirler. &amp;Ouml;rneğe ait &amp;ccedil;alışma zamanı &amp;ccedil;ıktısı aşağıdaki gibidir.&lt;/p&gt;
&lt;p&gt;&lt;img src="/image.axd?picture=/2025/Kasim/rust_exc_10.png" alt="" /&gt;&lt;/p&gt;
&lt;h2&gt;Daha Kapsamlı Test Senaryoları Yazmak&lt;/h2&gt;
&lt;p&gt;Kodun kalitesini ve doğruluğunu artırmak i&amp;ccedil;in kapsamlı test senaryoları yazmak &amp;ouml;nemlidir. Burada normal durumlar dışında &lt;strong&gt;u&amp;ccedil; vakalar&lt;em&gt;(edge cases)&lt;/em&gt; &lt;/strong&gt;ve hata senaryolarını da kapsayan testler yazılması &amp;ouml;nemlidir. S&amp;ouml;z gelimi bir sosyal sigorta g&amp;uuml;venlik numarasının doğruluğunu kontrol eden bir fonksiyon geliştirdiğimizi d&amp;uuml;ş&amp;uuml;nelim. Bu fonksiyon i&amp;ccedil;in sadece ge&amp;ccedil;erli numaraları değil, aynı zamanda format ihlallerini, eksik karakterleri ve diğer olası hata durumlarını da test etmeliyiz. Aşağıdaki &amp;ouml;rnek kod par&amp;ccedil;asında bu senaryo ele alınmaktadır. Kodun test edilmiş olması ve olası t&amp;uuml;m senaryolarının ele alınması aynı zamanda &lt;strong&gt;Code Coverage&lt;/strong&gt; değerini artıran ve dolayısıyla kodun kalitesini pozitif anlamda etkileyen &amp;ouml;nemli bir unsurdur.&lt;/p&gt;
&lt;pre class="brush:cpp;auto-links:false;toolbar:false" contenteditable="false"&gt;pub fn validate_social_security_number(ssn: &amp;amp;str) -&amp;gt; bool {
    // Basit bir doğrulama: SSN 9 haneli olmalı ve sadece rakamlardan oluşmalı
    let is_nine_digits = ssn.len() == 9;
    let all_digits = ssn.chars().all(|c| c.is_digit(10));
    is_nine_digits &amp;amp;&amp;amp; all_digits
}

#[cfg(test)]
mod tests {
    use super::*;

    // Normal durum testi
    #[test]
    fn test_valid_ssn() {
        assert!(validate_social_security_number("123456789"));
    }

    // Edge case testleri
    #[test]
    fn test_empty_or_whitespace_ssn() {
        assert!(!validate_social_security_number("")); // Boş string
        assert!(!validate_social_security_number("   ")); // Sadece boşluk
    }
    
    #[test]
    fn test_too_long_or_short_ssn() {
        assert!(!validate_social_security_number("123456789012345")); // &amp;Ccedil;ok uzun
        assert!(!validate_social_security_number("12345")); // &amp;Ccedil;ok kısa
    }

    // Hata Senaryosu/Negatif testleri
    #[test]
    fn test_invalid_format_ssn() {
        assert!(!validate_social_security_number("123-45-6789")); // Yanlış format
        assert!(!validate_social_security_number("12345678A")); // Harf i&amp;ccedil;eriyor
        assert!(!validate_social_security_number("12 3456789")); // Boşluk i&amp;ccedil;eriyor
    }

    #[test]
    fn test_right_length_but_wrong_characters_ssn() {
        assert!(!validate_social_security_number("12345A789")); // 8 haneli
    }
}&lt;/pre&gt;
&lt;p&gt;Bu kodun test &amp;ccedil;ıktıları da aşağıdaki gibi olacaktır.&lt;/p&gt;
&lt;p&gt;&lt;img src="/image.axd?picture=/2025/Kasim/rust_exc_11.png" alt="" /&gt;&lt;/p&gt;
&lt;h2&gt;Lazy Iterator Kullanımı ile Bellek Verimliliğini Artırmak&lt;/h2&gt;
&lt;p&gt;Rust, fonksiyonel dil &amp;ouml;zellikleri barındırır ve g&amp;uuml;&amp;ccedil;l&amp;uuml; iterator fonksiyonlarına sahiptir&lt;em&gt; (Hatta zero-cost abstraction s&amp;ouml;z konusudur ve dolayısıyla iteratif fonksiyonların maliyetleri olduk&amp;ccedil;a d&amp;uuml;ş&amp;uuml;kt&amp;uuml;r)&lt;/em&gt; &lt;strong&gt;map&lt;/strong&gt;, &lt;strong&gt;filter&lt;/strong&gt; ve &lt;strong&gt;collect&lt;/strong&gt; gibi birbirlerine bağlaran fonksiyonel bir akış oluşturan metotlar esasında &lt;strong&gt;next&lt;/strong&gt; işlevi &amp;ccedil;ağırılana kadar y&amp;uuml;r&amp;uuml;t&amp;uuml;lmezler. Bunu &lt;strong&gt;Lazy Evaluation&lt;/strong&gt; olarak ifade edebiliriz. Bu durumda gereksiz hesaplamaların &amp;ouml;n&amp;uuml;ne ge&amp;ccedil;ilerek bellek verimliliği artırılabilir. Elbette bunun tam tersi olarak birde &lt;strong&gt;Eager Evaluation&lt;/strong&gt; durumu vardır. Eager Evaluation senaryosunda t&amp;uuml;m veri &amp;uuml;zerinde işlemler hemen ger&amp;ccedil;ekleştirilir ve sonu&amp;ccedil;lar anında elde edilir. Ancak bu durum b&amp;uuml;y&amp;uuml;k veri setlerinde performans ve bellek kullanımı a&amp;ccedil;ısından dezavantajlı olabilir. &lt;span style="text-decoration: underline;"&gt;Dolayısıyla duruma g&amp;ouml;re Lazy veya Eager load stratejileri tercih edilebilir.&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&amp;Ccedil;ok b&amp;uuml;y&amp;uuml;k bir log dosyasından ham metin girdilerinin okunup analiz edildiği durumlarda &lt;strong&gt;Lazy Evaluation&lt;/strong&gt; ile bellek kullanımını daha optimize edebiliriz. Aşağıda bu senaryoya ilişkin basit bir kod par&amp;ccedil;ası yer almaktadır.&lt;/p&gt;
&lt;pre class="brush:cpp;auto-links:false;toolbar:false" contenteditable="false"&gt;fn main() {
    let log_data = vec![
        String::from("INFO: Application started"),
        String::from("ERROR: Failed to load configuration"),
        String::from("INFO: User logged in"),
        String::from("ERROR: Database connection lost"),
    ];

    println!("--- Lazy Evaluation Results ---");
    let error_logs = get_error_logs_lazy(&amp;amp;log_data);
    error_logs.iter().for_each(|log| println!("{}", log));

    println!("--- Eager Evaluation Results ---");
    let error_logs = get_error_logs_eager(&amp;amp;log_data);
    error_logs.iter().for_each(|log| println!("{}", log));
}

/// Basit bir log analiz fonksiyonu (Lazy Evaluation ile)
/// Log verisi alır ve "ERROR" i&amp;ccedil;eren satırları d&amp;ouml;ner
///
/// # Arguments
///
/// * `log_data` - Log verisi i&amp;ccedil;eren String vekt&amp;ouml;r&amp;uuml;
///
/// # Returns
///
/// * `impl Iterator&amp;lt;Item=String&amp;gt;` - "ERROR" i&amp;ccedil;eren log satırlarını &amp;uuml;reten iterator
fn get_error_logs_lazy(log_data: &amp;amp;[String]) -&amp;gt; Vec&amp;lt;String&amp;gt; {
    /*
        Bu yaklaşımda Lazy Evaluation kullanılmaktadır.
        Log verisi &amp;uuml;zerinde bir iterator oluşturulur ve
        "ERROR" i&amp;ccedil;eren satırlar filtrelenir.
        Bu sayede gereksiz yere t&amp;uuml;m veriyi işlemekten ka&amp;ccedil;ınılır.
    */
    log_data
        .into_iter()
        .filter(|line| line.contains("ERROR"))
        .map(|line| {
            let columns = line.split(": ").collect::&amp;lt;Vec&amp;lt;&amp;amp;str&amp;gt;&amp;gt;();
            format!(
                "Critical Error Found: {}",
                columns.last().unwrap_or(&amp;amp;"Unknown Error")
            )
        })
        .collect()
}

/// Basit bir log analiz fonksiyonu (Eager Evaluation ile)
/// Log verisi alır ve "ERROR" i&amp;ccedil;eren satırları d&amp;ouml;ner
///
/// # Arguments
///
/// * `log_data` - Log verisi i&amp;ccedil;eren String vekt&amp;ouml;r&amp;uuml;
///
/// # Returns
///
/// * `Vec&amp;lt;String&amp;gt;` - "ERROR" i&amp;ccedil;eren log satırlarını i&amp;ccedil;eren vekt&amp;ouml;r
fn get_error_logs_eager(log_data: &amp;amp;[String]) -&amp;gt; Vec&amp;lt;String&amp;gt; {
    /*
        Bu yaklaşımda Eager Evaluation kullanılmaktadır.
        T&amp;uuml;m log verisi işlenir ve "ERROR" i&amp;ccedil;eren satırlar
        hemen d&amp;ouml;nd&amp;uuml;r&amp;uuml;l&amp;uuml;r.
    */
    let mut error_logs = Vec::new();
    for line in log_data {
        if line.contains("ERROR") {
            let columns: Vec&amp;lt;&amp;amp;str&amp;gt; = line.split(": ").collect();
            let formatted_log = format!(
                "Critical Error Found: {}",
                columns.last().unwrap_or(&amp;amp;"Unknown Error")
            );
            error_logs.push(formatted_log);
        }
    }
    error_logs
}&lt;/pre&gt;
&lt;p&gt;Bu &amp;ouml;rnek elbette performans farkı ve &amp;ccedil;alışma zamanı bellek t&amp;uuml;ketim maliyetlerini g&amp;ouml;stermez ancak &lt;strong&gt;Lazy&lt;/strong&gt; ve &lt;strong&gt;Eager&lt;/strong&gt;&amp;nbsp;&lt;strong&gt;Loading&lt;/strong&gt; senaryoları i&amp;ccedil;in kodu nasıl kullanacağımızı a&amp;ccedil;ıklar. &amp;Ccedil;alışma zamanı &amp;ccedil;ıktısını da buraya bırakalım. Zira siz denediğinizde de benzer sonu&amp;ccedil;lar almalısınız.&lt;/p&gt;
&lt;p&gt;&lt;img src="/image.axd?picture=/2025/Kasim/rust_exc_12.png" alt="" /&gt;&lt;/p&gt;
&lt;h2&gt;Generic T&amp;uuml;rlerde Kısıtlamaları&lt;em&gt;(Constraint)&lt;/em&gt; Kullanmak&lt;/h2&gt;
&lt;p&gt;Generic t&amp;uuml;rlerin kullanıldığı durumlarda t&amp;uuml;r&amp;uuml; belli davranışları uygulamaya zorlamak i&amp;ccedil;in trait'lerden yararlanılabilir. B&amp;ouml;ylece &amp;ouml;rneğin bir iterasyonun aynı davranış veya davranışlara sahip t&amp;uuml;rler ile &amp;ccedil;alışması sağlanabilir. Dolayısıyla tip sistemini kullanarak işlevselliği bir nevi garanti altına almış oluruz ve bunu sıfır maliyetle yaparız.&lt;/p&gt;
&lt;p&gt;Herhangi bir t&amp;uuml;r i&amp;ccedil;in minimum ve maksimum değerleri bulan bir fonksiyon geliştirdiğimizi d&amp;uuml;ş&amp;uuml;nelim. Bunun i&amp;ccedil;in t&amp;uuml;r&amp;uuml;n karşılaştırılabilir &lt;strong&gt;Ord&lt;/strong&gt; ve kopyalanabilir &lt;strong&gt;Copy&lt;/strong&gt; olması gerekir. Aksi takdirde fonksiyon doğru &amp;ccedil;alışmayacaktır. Bunu sağlamak i&amp;ccedil;in generic t&amp;uuml;r &amp;uuml;zerinde trait kısıtlamaları kullanabiliriz. Aşağıdaki &amp;ouml;rnek kod par&amp;ccedil;asında bu durum ele alınmaktadır.&lt;/p&gt;
&lt;pre class="brush:cpp;auto-links:false;toolbar:false" contenteditable="false"&gt;fn main() {
    let numbers = vec![3, 1, 4, 1, 5, 9, 2, 6, 5, 3, 5];
    match find_min_max(&amp;amp;numbers) {
        Some((min, max)) =&amp;gt; {
            println!("Minimum: {}, Maximum: {}", min, max);
        }
        None =&amp;gt; {
            println!("Empty slice provided.");
        }
    }

    let chars = vec!['y', 'c', 'm', 'e', 'q', 'l', 'x', 'k'];
    match find_min_max(&amp;amp;chars) {
        Some((min, max)) =&amp;gt; {
            println!("Minimum: {}, Maximum: {}", min, max);
        }
        None =&amp;gt; {
            println!("Empty slice provided.");
        }
    }

    let towers = vec![
        Tower { height: 150 },
        Tower { height: 200 },
        Tower { height: 175 },
    ];
    match find_min_max(&amp;amp;towers) {
        Some((min, max)) =&amp;gt; {
            println!(
                "Minimum Tower Height: {}, Maximum Tower Height: {}",
                min.height, max.height
            );
        }
        None =&amp;gt; {
            println!("Empty slice provided.");
        }
    }
}

/// Verilen bir slice i&amp;ccedil;indeki minimum ve maksimum değerleri bulan fonksiyon.
/// Eğer slice boşsa None d&amp;ouml;ner, aksi takdirde Some((min, max)) d&amp;ouml;ner.
///
/// # Arguments
/// * `values` - Karşılaştırılacak değerlerin bulunduğu slice.
///
/// # Returns
/// * `Option&amp;lt;(T, T)&amp;gt;` - Minimum ve maksimum değerleri i&amp;ccedil;eren bir tuple veya None.
///
/// # Constraints
/// * `T: Ord + Copy` - T t&amp;uuml;r&amp;uuml; karşılaştırılabilir ve kopyalanabilir olmalıdır.
fn find_min_max&amp;lt;T: Ord + Copy&amp;gt;(values: &amp;amp;[T]) -&amp;gt; Option&amp;lt;(T, T)&amp;gt; {
    if values.is_empty() {
        return None;
    }

    let mut min = values[0];
    let mut max = values[0];

    for &amp;amp;value in values.iter() {
        if value &amp;lt; min {
            min = value;
        }
        if value &amp;gt; max {
            max = value;
        }
    }

    Some((min, max))
}

#[derive(Copy, Clone, Eq, PartialEq)]
struct Tower {
    height: u32,
}

impl PartialOrd for Tower {
    fn partial_cmp(&amp;amp;self, other: &amp;amp;Self) -&amp;gt; Option&amp;lt;std::cmp::Ordering&amp;gt; {
        Some(self.cmp(other))
    }
}

impl Ord for Tower {
    fn cmp(&amp;amp;self, other: &amp;amp;Self) -&amp;gt; std::cmp::Ordering {
        self.height.cmp(&amp;amp;other.height)
    }
}&lt;/pre&gt;
&lt;p&gt;&amp;Ouml;rnekte yer alan &lt;strong&gt;find_min_max&lt;/strong&gt; fonksiyonu &lt;strong&gt;T&lt;/strong&gt; t&amp;uuml;r&amp;uuml;nden bir dizi referansı&amp;nbsp;almakta ve yine aynı t&amp;uuml;rden bir &lt;strong&gt;Tuple&lt;/strong&gt; d&amp;ouml;nd&amp;uuml;rmektedir. Fonksiyonda&lt;strong&gt; T&lt;/strong&gt; t&amp;uuml;r&amp;uuml; i&amp;ccedil;in &lt;strong&gt;Ord&lt;/strong&gt; ve &lt;strong&gt;Copy&lt;/strong&gt; trait'lerini uygulama zorunluluğu getirilmiştir. Buna g&amp;ouml;re primitive tipler'den tutun da kendi yazdığımız t&amp;uuml;rler i&amp;ccedil;in de aynı fonksiyonu kullanabiliriz. Tek şart kendi t&amp;uuml;r&amp;uuml;m&amp;uuml;z&amp;uuml;n de bu &lt;strong&gt;trait&lt;/strong&gt;'leri uygulamış olmasıdır. &amp;Ouml;rnek kodda yer alan &lt;strong&gt;Tower&lt;/strong&gt; veri yapısı a&amp;ccedil;ık bir şekilde &lt;strong&gt;PartialOrd&lt;/strong&gt; ve &lt;strong&gt;Ord&lt;/strong&gt; trait'lerini uygulamaktadır. Bu sayede karşılaştırılabilme davranışını kazanmıştır. Ayrıca &lt;strong&gt;derive&lt;/strong&gt; niteliği &amp;uuml;zerinden doğal yolla &lt;strong&gt;Copy&lt;/strong&gt; trait'ini implemente etmektedir. &amp;Ouml;rneğin &amp;ccedil;alışma zamanı &amp;ccedil;ıktısı aşağıda g&amp;ouml;r&amp;uuml;ld&amp;uuml;ğ&amp;uuml; gibidir.&lt;/p&gt;
&lt;p&gt;&lt;img src="/image.axd?picture=/2025/Kasim/rust_exc_13.png" alt="" /&gt;&lt;/p&gt;
&lt;h2&gt;Daha G&amp;uuml;&amp;ccedil;l&amp;uuml; Hata Y&amp;ouml;netimi i&amp;ccedil;in Custom Error T&amp;uuml;rleri Oluşturmak veya thiserror Kullanmak&lt;/h2&gt;
&lt;p&gt;Uygulamalarda hata y&amp;ouml;netimi kritik bir &amp;ouml;neme sahiptir. &lt;strong&gt;I/O&lt;/strong&gt; işlemleri, &lt;strong&gt;network&lt;/strong&gt; operasyoları, &lt;strong&gt;veri tabanı&lt;/strong&gt; erişimleri, dosya okuma yazma vb işlemler sırasında &amp;ccedil;eşitli hatalar meydana gelebilir. Rust'ın standart k&amp;uuml;t&amp;uuml;phanesi hata y&amp;ouml;netimi i&amp;ccedil;in &lt;strong&gt;Result&lt;/strong&gt; t&amp;uuml;r&amp;uuml;n&amp;uuml; sağlar ancak daha karmaşık senaryolarda &amp;ouml;zel hata t&amp;uuml;rleri oluşturmak gerekir. Burada genellikle kendi &lt;strong&gt;enum&lt;/strong&gt; t&amp;uuml;rlerimizi kullanırız ama idiomtik olarak t&amp;uuml;m olası hataları modelleyen &lt;strong&gt;thiserror&lt;/strong&gt; gibi neredeyse bir hata y&amp;ouml;netim standardı olmuş k&amp;uuml;t&amp;uuml;phaneleri de kullanabiliriz. Aşağıdaki &amp;ouml;rnek kod par&amp;ccedil;asında bir hata y&amp;ouml;netimi senaryosu ele alınmaktadır.&lt;/p&gt;
&lt;pre class="brush:cpp;auto-links:false;toolbar:false" contenteditable="false"&gt;use serde::Deserialize;
use std::{fs, io};
use thiserror::Error;

fn main() -&amp;gt; Result&amp;lt;(), ApiError&amp;gt; {
    let settings = load_settings("config.json");
    match settings {
        Ok(cfg) =&amp;gt; {
            println!("Settings loaded: {:?}", cfg);
        }
        Err(e) =&amp;gt; {
            eprintln!("Error loading settings: {}", e);
        }
    }

    let ping_result = send_ping("localhost:67000");
    match ping_result {
        Ok(_) =&amp;gt; println!("Ping successful!"),
        Err(e) =&amp;gt; eprintln!("Error sending ping: {}", e),
    }

    Ok(())
}

#[derive(Error, Debug)]
pub enum ApiError {
    // io:Error t&amp;uuml;r&amp;uuml;ndeki hataları otomatik olarak ApiError::Io varyantına d&amp;ouml;n&amp;uuml;şt&amp;uuml;r&amp;uuml;r.
    #[error("I/O Error: {0}")]
    Io(#[from] io::Error),

    // Ağ ile ilgili hataları temsil eder.
    #[error("Network Error: {0}")]
    Network(String),

    // JSON serileştirme/deserileştirme hatalarını temsil eder.
    #[error("JSON Error: {0}")]
    Json(#[from] serde_json::Error),
}

fn load_settings(path: &amp;amp;str) -&amp;gt; Result&amp;lt;Settings, ApiError&amp;gt; {
    let data = fs::read_to_string(path)?; // io::Error otomatik olarak ApiError::Io'ya d&amp;ouml;n&amp;uuml;şt&amp;uuml;r&amp;uuml;l&amp;uuml;r
    let settings: Settings = serde_json::from_str(&amp;amp;data)?; // serde_json::Error otomatik olarak ApiError::Json'a d&amp;ouml;n&amp;uuml;şt&amp;uuml;r&amp;uuml;l&amp;uuml;r
    Ok(settings)
}

fn send_ping(api_url: &amp;amp;str) -&amp;gt; Result&amp;lt;(), ApiError&amp;gt; {
    let response = std::net::TcpStream::connect(api_url);
    match response {
        Ok(_) =&amp;gt; println!("Ping to {} successful!", api_url),
        Err(e) =&amp;gt; return Err(ApiError::Network(e.to_string())),
    }
    Ok(())
}

#[derive(Deserialize, Debug)]
#[allow(dead_code)]
struct Settings {
    api_url: String,
    timeout: u64,
}&lt;/pre&gt;
&lt;p&gt;Burada hata durumları i&amp;ccedil;in &lt;strong&gt;ApiError&lt;/strong&gt; isimli &lt;strong&gt;enum&lt;/strong&gt; t&amp;uuml;r&amp;uuml;nden bir veri yapısı kullanılmaktadır. Dikkat edileceği &amp;uuml;zere bu enum yapısı &lt;strong&gt;Error&lt;/strong&gt; ve &lt;strong&gt;error&lt;/strong&gt; nitelikleri ile donatılmıştır. &lt;strong&gt;thiserror&lt;/strong&gt; crate'inden gelen bu nitelikler ile farklı hata t&amp;uuml;rleri kendi standart hata t&amp;uuml;r&amp;uuml;m&amp;uuml;ze kolayca &amp;ccedil;evrilebilir ve bu uygulama genelinde bir standartlık sağlar. S&amp;ouml;z gelimi &lt;strong&gt;io::Error&lt;/strong&gt; t&amp;uuml;r&amp;uuml;nden bir hata oluştuğunda bu otomatik olarak &lt;strong&gt;ApiError:Io&lt;/strong&gt;'ya evrilir. &lt;strong&gt;Result&lt;/strong&gt; &amp;uuml;zerinden &lt;strong&gt;Error&lt;/strong&gt; d&amp;ouml;nme potansiyeli olan her yerde &lt;strong&gt;ApiError&lt;/strong&gt;&amp;nbsp;veri yapımız ile &amp;ccedil;alışabilir ve genelleştirdiğimiz hataları ele alabiliriz.&lt;/p&gt;
&lt;p&gt;Tabii bu &amp;ouml;rneği &amp;ccedil;alıştırmak i&amp;ccedil;in gerekli crate'lerin projeye y&amp;uuml;klenmiş olması gerektiğini hatırlatalım.&lt;/p&gt;
&lt;pre class="brush:plain;auto-links:false;toolbar:false" contenteditable="false"&gt;[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
thiserror = "2.0.17"&lt;/pre&gt;
&lt;p&gt;Konu kapsamında &lt;strong&gt;thiserror&lt;/strong&gt; &lt;strong&gt;crate&lt;/strong&gt;'ine odaklanmak gerekir. &lt;strong&gt;serde&lt;/strong&gt; ve &lt;strong&gt;serde_json&lt;/strong&gt; k&amp;uuml;feleri sadece &lt;strong&gt;JSON&lt;/strong&gt; bazlı &amp;ouml;rnekler i&amp;ccedil;in eklenmiştir.&amp;nbsp;&lt;/p&gt;
&lt;p&gt;&lt;img src="/image.axd?picture=/2025/Kasim/rust_exc_14.png" alt="" /&gt;&lt;/p&gt;
&lt;h2&gt;Tip D&amp;ouml;n&amp;uuml;ş&amp;uuml;mlerinde From ve Into Trait'lerini Kullanmak&lt;/h2&gt;
&lt;p&gt;Rust dilinde tip d&amp;ouml;n&amp;uuml;ş&amp;uuml;mleri i&amp;ccedil;in genellikle &lt;strong&gt;From&lt;/strong&gt; ve &lt;strong&gt;Into&lt;/strong&gt; trait'leri kullanılır. Bu trait'ler, bir t&amp;uuml;r&amp;uuml;n başka bir t&amp;uuml;re d&amp;ouml;n&amp;uuml;şt&amp;uuml;r&amp;uuml;lmesini sağlar. From trait'i, bir t&amp;uuml;rden diğerine d&amp;ouml;n&amp;uuml;ş&amp;uuml;m i&amp;ccedil;in bir y&amp;ouml;ntem tanımlar. Bu trait uygulandığında otomatik olarak &lt;strong&gt;Into&lt;/strong&gt; trait'i de uygulanmış olur. Yani bir t&amp;uuml;rden diğerine d&amp;ouml;n&amp;uuml;ş&amp;uuml;m yapmak i&amp;ccedil;in ya &lt;strong&gt;From&lt;/strong&gt; ya da &lt;strong&gt;Into&lt;/strong&gt;&amp;nbsp;uyarlamaları kullanılabilir. S&amp;ouml;z konusu trait'ler Ownership&lt;em&gt;(sahiplik)&lt;/em&gt; ve bor&amp;ccedil;lanma&lt;em&gt;(borrowing)&lt;/em&gt; kuralları ile uyumlu &amp;ccedil;alışımasını sağlar ve dolayısıyla d&amp;ouml;n&amp;uuml;ş&amp;uuml;mler g&amp;uuml;venli bir şekilde ger&amp;ccedil;ekleştirilir. Ayrıca Rust bu d&amp;ouml;n&amp;uuml;ş&amp;uuml;mleri optimize edebilir ve gereksiz kopyalamaları &amp;ouml;nleyebilir.&lt;/p&gt;
&lt;p&gt;Uygulama seviyesindeki hataları temsil eden bir &lt;strong&gt;enum&lt;/strong&gt; t&amp;uuml;r&amp;uuml;m&amp;uuml;z olduğunu d&amp;uuml;ş&amp;uuml;nelim. Bazı i&amp;ccedil; fonksiyonlardan da &lt;strong&gt;Result&lt;/strong&gt; t&amp;uuml;r&amp;uuml;nde bu enum t&amp;uuml;r&amp;uuml; ile hata d&amp;ouml;n&amp;uuml;yor olsun. Diğer hata t&amp;uuml;rlerinden bu &lt;strong&gt;enum&lt;/strong&gt; t&amp;uuml;r&amp;uuml;ne d&amp;ouml;n&amp;uuml;ş&amp;uuml;m yapabilmek i&amp;ccedil;in &lt;strong&gt;From&lt;/strong&gt; trait'ini kullanabiliriz. &amp;Ouml;rneğin bir&lt;strong&gt; I/O&lt;/strong&gt; hatası yakalandıysa ve bunu katmanlara &amp;ccedil;ıkarken kendi hata t&amp;uuml;r&amp;uuml;m&amp;uuml;ze d&amp;ouml;n&amp;uuml;şt&amp;uuml;rmek istiyorsak &lt;strong&gt;From&lt;/strong&gt; trait'ini uygulayabiliriz. Bu senaryoda &lt;strong&gt;?&lt;/strong&gt; operat&amp;ouml;r&amp;uuml; de otomatikman &amp;ccedil;alışacak ve t&amp;uuml;rler arasında d&amp;ouml;n&amp;uuml;ş&amp;uuml;m sağlanacaktır. Bir nevi &amp;ouml;nceki &amp;ouml;rnekte kullandığımız thiserror crate ile ele aldığımız senaryoyu işletebileceğimizi ifade edebiliriz. Aşağıdaki &amp;ouml;rnek kod par&amp;ccedil;asında bu senaryo ele alınmaktadır.&lt;/p&gt;
&lt;pre class="brush:cpp;auto-links:false;toolbar:false" contenteditable="false"&gt;use std::fs::File;
use std::io;
use std::num;

fn main() {
    let result = search("WARNING");
    match result {
        Ok(content) =&amp;gt; println!("Search successful: {}", content),
        Err(e) =&amp;gt; match e {
            AppError::Io(err) =&amp;gt; eprintln!("I/O Error: {}", err),
            AppError::Parse(err) =&amp;gt; eprintln!("Parse Error: {}", err),
            AppError::Auth(msg) =&amp;gt; eprintln!("Authentication Error: {}", msg),
            AppError::NotFound(msg) =&amp;gt; eprintln!("Not Found Error: {}", msg),
        },
    }

    let num_str = "32a14";
    if let Err(e) = parse_number(num_str) {
        match e {
            AppError::Parse(err) =&amp;gt; eprintln!("Failed to parse number: {}", err),
            _ =&amp;gt; eprintln!("An unexpected error occurred"),
        }
    }

    // into kullanımı ile de t&amp;uuml;rler arası d&amp;ouml;n&amp;uuml;ş&amp;uuml;m yapılabilir
    // Burada io::Error t&amp;uuml;r&amp;uuml;ndeki hata AppError t&amp;uuml;r&amp;uuml;ne d&amp;ouml;n&amp;uuml;şt&amp;uuml;r&amp;uuml;lmektedir
    let io_error = io::Error::new(io::ErrorKind::Other, "an I/O error occurred");
    let app_error: AppError = io_error.into();
    match app_error {
        AppError::Io(err) =&amp;gt; eprintln!("Converted I/O Error: {}", err),
        _ =&amp;gt; eprintln!("An unexpected error occurred"),
    }
}

fn parse_number(s: &amp;amp;str) -&amp;gt; Result&amp;lt;i32, AppError&amp;gt; {
    /*
    parse fonksiyonu str t&amp;uuml;r&amp;uuml;ndeki bir veriyi i32 t&amp;uuml;r&amp;uuml;ne d&amp;ouml;n&amp;uuml;şt&amp;uuml;rmeye &amp;ccedil;alışır.
    Eğer d&amp;ouml;n&amp;uuml;ş&amp;uuml;m başarılı olursa, Ok(num) d&amp;ouml;ner aksi durumda ParseIntError t&amp;uuml;r&amp;uuml;nde bir hata oluşur.
    From trait implemente edildiği i&amp;ccedil;in bu hata AppError t&amp;uuml;r&amp;uuml;ne otomatikman d&amp;ouml;n&amp;uuml;şt&amp;uuml;r&amp;uuml;l&amp;uuml;r.
    */
    let num: i32 = s.parse()?;
    Ok(num)
}

fn search(query: &amp;amp;str) -&amp;gt; Result&amp;lt;String, AppError&amp;gt; {
    /*
    Bu fonksiyon belirtilen dosyayı a&amp;ccedil;ar ve i&amp;ccedil;eriğinde query parametresi ile gelen veriyi arar.
    Eğer dosya a&amp;ccedil;ılamazsa io::Error t&amp;uuml;r&amp;uuml;nde bir hata oluşur ve bu hata AppError t&amp;uuml;r&amp;uuml;ne d&amp;ouml;n&amp;uuml;şt&amp;uuml;r&amp;uuml;l&amp;uuml;r.
    Zira, From trait implemente edilmiştir.
    */
    let f = File::open("games.dat")?;
    println!("File opened successfully: {:?}", f);
    println!("Searching for query: {}", query);
    Ok(String::from("Content found"))
}

#[allow(dead_code)]
#[derive(Debug)]
enum AppError {
    Io(io::Error),
    Parse(num::ParseIntError),
    Auth(String),
    NotFound(String),
}

impl From&amp;lt;io::Error&amp;gt; for AppError {
    fn from(error: io::Error) -&amp;gt; Self {
        AppError::Io(error)
    }
}

impl From&amp;lt;num::ParseIntError&amp;gt; for AppError {
    fn from(error: num::ParseIntError) -&amp;gt; Self {
        AppError::Parse(error)
    }
}&lt;/pre&gt;
&lt;p&gt;Dikkat edileceği &amp;uuml;zere &lt;strong&gt;io&lt;/strong&gt; k&amp;uuml;t&amp;uuml;phanesinden &lt;strong&gt;Error&lt;/strong&gt; ve &lt;strong&gt;num&lt;/strong&gt; k&amp;uuml;t&amp;uuml;phanesinden &lt;strong&gt;ParseIntError&lt;/strong&gt; t&amp;uuml;rlerinin &lt;strong&gt;AppError&lt;/strong&gt; t&amp;uuml;r&amp;uuml;ne d&amp;ouml;n&amp;uuml;şmesi i&amp;ccedil;in birer uyarlama s&amp;ouml;z konusu. Buna g&amp;ouml;re &lt;strong&gt;search&lt;/strong&gt; ve &lt;strong&gt;parse_number&lt;/strong&gt; fonksiyonlarında meycana gelebilecek ve &lt;strong&gt;?&lt;/strong&gt; operat&amp;ouml;r&amp;uuml; ile otomatik olarak yakalanacak bu hatalar otomatik olarak AppError t&amp;uuml;r&amp;uuml;ne d&amp;ouml;n&amp;uuml;şt&amp;uuml;r&amp;uuml;lebilir. Kodun &amp;ccedil;alışma zamanı &amp;ccedil;ıktısı aşağıdaki gibi olacaktır.&lt;/p&gt;
&lt;p&gt;&lt;img src="/image.axd?picture=/2025/Kasim/rust_exc_15.png" alt="" /&gt;&lt;/p&gt;
&lt;h2&gt;Generic Trait'lerde Associated Types Kullanımı&lt;/h2&gt;
&lt;p&gt;Trait'lerde generic t&amp;uuml;r parametreleri kullanmak yerine ilişkili tipler&lt;em&gt;(associated types)&lt;/em&gt; kullanarak soyutlamalar yapabiliriz. Bunu yaparak trait'in uygulamadaki kullanımında sadece tek bir somut t&amp;uuml;r ile &amp;ccedil;alışmasını garanti ederiz. &amp;Ouml;rneğin bir veri deposu&lt;em&gt;(data store)&lt;/em&gt; soyutlaması tasarladığımızı d&amp;uuml;ş&amp;uuml;nelim. Bu soyutlama farklı veri t&amp;uuml;rleri ile &amp;ccedil;alışabilir ancak her bir veri deposu uyarlaması sadece tek bir veri t&amp;uuml;r&amp;uuml; ile &amp;ccedil;alışmalıdır. Bu durumda generic t&amp;uuml;r parametreleri yerine ilişkili tipler kullanmak daha uygun olacaktır. Aşağıdaki kod par&amp;ccedil;asında her iki yaklaşımın kullanımına ait &amp;ouml;rnek kodlar yer almaktadır.&lt;/p&gt;
&lt;pre class="brush:cpp;auto-links:false;toolbar:false" contenteditable="false"&gt;/*
    DataStore trait'i basit bir veri deposu soyutlaması sağlar.
    Associated type ile veri tipinin belirtilmesi zorunlu kılınır.

    InMemoryStore yapısı, string t&amp;uuml;r&amp;uuml;nden verileri bellekte saklayan bir veri deposu.
    DataStore trait'ini implemente ediyor ve ilişkili tip olarak String kullanacağını somut bir şekilde belirtiyor.
*/
#[allow(dead_code)]
trait DataStore {
    type Item; // Associated type tanımı

    fn save(&amp;amp;mut self, item: Self::Item);
    fn read(&amp;amp;self, id: u32) -&amp;gt; Option&amp;lt;Self::Item&amp;gt;;
}
struct InMemoryStore {
    items: Vec&amp;lt;String&amp;gt;,
}
impl DataStore for InMemoryStore {
    type Item = String; // Somut t&amp;uuml;r belirtimi. Artık Item t&amp;uuml;r&amp;uuml; String dolayısıyla InMemoryStore sadece String t&amp;uuml;r&amp;uuml;nden verilerle &amp;ccedil;alışır.

    fn save(&amp;amp;mut self, item: Self::Item) {
        self.items.push(item);
    }

    fn read(&amp;amp;self, id: u32) -&amp;gt; Option&amp;lt;Self::Item&amp;gt; {
        self.items.get(id as usize).cloned()
    }
}

/*
    Generic t&amp;uuml;r parametreleri ile aynı soyutlamayı yapıyoruz.
    Burada ilişkili tip yerine generic t&amp;uuml;r parametresi T kullanılıyor.
    Dikkat edileceği &amp;uuml;zere type şeklinde bir tanımlama yok. Dolayısıyla bu trait'i implemente etmek isteyen bir yapı,
    hangi t&amp;uuml;r&amp;uuml; kullanacağını her seferinde belirtmek zorunda. Elbette bazen bu esnekliğe ihtiya&amp;ccedil; duyuyoruz.
*/
#[allow(dead_code)]
trait GenericDataStore&amp;lt;T&amp;gt; {
    fn save(&amp;amp;mut self, item: T);
    fn read(&amp;amp;self, id: u32) -&amp;gt; Option&amp;lt;T&amp;gt;;
}
struct GenericInMemoryStore&amp;lt;T&amp;gt; {
    items: Vec&amp;lt;T&amp;gt;,
}

impl&amp;lt;T: Clone&amp;gt; GenericDataStore&amp;lt;T&amp;gt; for GenericInMemoryStore&amp;lt;T&amp;gt; {
    fn save(&amp;amp;mut self, item: T) {
        self.items.push(item);
    }

    fn read(&amp;amp;self, id: u32) -&amp;gt; Option&amp;lt;T&amp;gt; {
        self.items.get(id as usize).cloned()
    }
}

fn main() {
    let mut store = InMemoryStore { items: vec![] };
    store.save("connection string".to_string());
    store.save("minio address".to_string());
    for item in store.items.iter() {
        println!("Loaded from InMemoryStore: {}", item);
    }

    let mut generic_store = GenericInMemoryStore { items: vec![] };
    generic_store.save(42);
    generic_store.save(100);
    if let Some(item) = generic_store.read(1) {
        println!("Loaded from GenericInMemoryStore: {}", item);
    }
}&lt;/pre&gt;
&lt;p&gt;&amp;Ouml;rneğin &amp;ccedil;alışma zamanı &amp;ccedil;ıktısı aşağıdaki gibidir.&lt;/p&gt;
&lt;p&gt;&lt;img src="/image.axd?picture=/2025/Kasim/rust_exc_16.png" alt="" /&gt;&lt;/p&gt;
&lt;h2&gt;Iterator Adapt&amp;ouml;rleri ve collect Kullanımı&lt;/h2&gt;
&lt;p&gt;Rust'ın fonksiyonel programlama &amp;ouml;zelliklerinden biri olan iterator adapt&amp;ouml;rleri akan veri &amp;uuml;zerinde işlem yapmamızı kolaylaştırır. Bunu yaparken d&amp;ouml;ng&amp;uuml;sel yapılar kurmamıza gerek kalmaz. T&amp;uuml;m operasyonu zincir metotlar &amp;uuml;zerinden halledebiliriz. &lt;strong&gt;Collect&lt;/strong&gt;, &lt;strong&gt;fold&lt;/strong&gt;, &lt;strong&gt;reduce&lt;/strong&gt;, &lt;strong&gt;find&lt;/strong&gt;, &lt;strong&gt;any&lt;/strong&gt;, &lt;strong&gt;all&lt;/strong&gt; gibi pek &amp;ccedil;ok adapt&amp;ouml;r metodu mevcuttur. Bu adapt&amp;ouml;rler sayesinde veriyi filtreleyebilir, d&amp;ouml;n&amp;uuml;şt&amp;uuml;rebilir, toplayabilir veya belirli koşullara g&amp;ouml;re sorgulayabiliriz.&lt;/p&gt;
&lt;p&gt;Bu adapt&amp;ouml;rlerden birisi olan &lt;strong&gt;collect&lt;/strong&gt; en &amp;ccedil;ok kullanılanlar arasındadır. Standart bir iterator zinciri &amp;uuml;zerinden elde edilen veriyi farklı koleksiyon t&amp;uuml;rlerine d&amp;ouml;n&amp;uuml;şt&amp;uuml;rmek i&amp;ccedil;in kullanılır. &amp;Ouml;rneğin bir vekt&amp;ouml;r i&amp;ccedil;indeki sayıları filtreleyip bir &lt;strong&gt;HashSet&lt;/strong&gt; veya başka bir veri yapısına d&amp;ouml;n&amp;uuml;şt&amp;uuml;rmek istediğimizde &lt;strong&gt;collect&lt;/strong&gt; metodunu kullanabiliriz. &lt;strong&gt;Collect&lt;/strong&gt;&amp;nbsp;&amp;ccedil;ağrısı sonucu bir değişkene atanabilir ve burada d&amp;ouml;n&amp;uuml;ş t&amp;uuml;r&amp;uuml; derleyici tarafından otomatik olarak tahmin edilebilir&lt;em&gt;(type inference)&lt;/em&gt; ama bazen d&amp;ouml;n&amp;uuml;ş t&amp;uuml;r&amp;uuml;n&amp;uuml;n a&amp;ccedil;ık&amp;ccedil;a belirtilmesi gerekir. Aşağıdaki kod par&amp;ccedil;asında &lt;strong&gt;collect&lt;/strong&gt; kullanımına dair birka&amp;ccedil; &amp;ouml;rnek bulunmaktadır.&lt;/p&gt;
&lt;pre class="brush:cpp;auto-links:false;toolbar:false" contenteditable="false"&gt;use rand::Rng;

fn main() {
    // 10 adet rastgele sayı &amp;uuml;retimi (map ile birlikte kullanım)
    let mut rng = rand::rng();
    let numbers: Vec&amp;lt;i32&amp;gt; = (0..10).map(|_| rng.random_range(1..101)).collect();
    println!("Random 10 numbers: {:?}", numbers);

    // Rastgele &amp;uuml;retilmiş olan sayılardan &amp;ccedil;ift olanların filtrelenmesi (filter ile birlikte kullanım)
    let even_numbers: Vec&amp;lt;i32&amp;gt; = numbers.into_iter().filter(|&amp;amp;x| x % 2 == 0).collect();
    println!("Even numbers: {:?}", even_numbers);

    // Bir sayı dizisindeki asal sayıların listesi ve toplam sayısı (filter ile birlikte kullanım)
    let numbers: Vec&amp;lt;i32&amp;gt; = (0..20).map(|_| rng.random_range(1..101)).collect();
    let primes: Vec&amp;lt;i32&amp;gt; = numbers.into_iter().filter(|&amp;amp;x| is_prime(x)).collect();
    println!("Prime numbers: {:?}", primes);
    println!("Count of prime numbers: {}", primes.len());

    // 8 adet g&amp;uuml;&amp;ccedil;leri 2 ile 5 arasında değişen AIPlayer nesneleri oluşturulması
    let ai_players: Vec&amp;lt;AIPlayer&amp;gt; = (0..8)
        .map(|i| AIPlayer {
            name: format!("AI_Player_{}", i + 1),
            power: rng.random_range(2..6),
        })
        .collect();

    // Bu oyunculardan g&amp;uuml;c&amp;uuml; 4'ten b&amp;uuml;y&amp;uuml;k olanların rastgele bir lokasyona atanması
    let strong_ai_locations: Vec&amp;lt;(AIPlayer, Location)&amp;gt; = ai_players
        .into_iter()
        .filter(|player| player.power &amp;gt; 4)
        .map(|player| {
            let location = Location {
                x: rng.random_range(0.0..100.0),
                y: rng.random_range(0.0..100.0),
            };
            (player, location)
        })
        .collect();

    println!("Strong AI Players and their Locations:");
    strong_ai_locations.iter().for_each(|(player, location)| {
        println!(
            "{} (Power: {}) is at Location ({:.2}, {:.2})",
            player.name, player.power, location.x, location.y
        );
    });
}

fn is_prime(num: i32) -&amp;gt; bool {
    if num &amp;lt;= 1 {
        return false;
    }

    for i in 2..=((num as f64).sqrt() as i32) {
        if num % i == 0 {
            return false;
        }
    }

    true
}

#[allow(dead_code)]
#[derive(Debug)]
struct AIPlayer {
    name: String,
    power: u16,
}

#[derive(Debug)]
struct Location {
    x: f64,
    y: f64,
}&lt;/pre&gt;
&lt;p&gt;Kodun &amp;ccedil;alışma zamanı &amp;ccedil;ıktısı aşağıdaki gibidir.&lt;/p&gt;
&lt;p&gt;&lt;img src="/image.axd?picture=/2025/Kasim/rust_exc_17.png" alt="" /&gt;&lt;/p&gt;
&lt;h2&gt;Module Gizleme ve Erişim Kontrol&amp;uuml;&lt;/h2&gt;
&lt;p&gt;Mod&amp;uuml;ller alan adı&lt;em&gt;(namespace)&lt;/em&gt; ve gizlilik sınırı&lt;em&gt;(privacy boundary)&lt;/em&gt; işlevi sağlar. Benzer amaca y&amp;ouml;nelik enstr&amp;uuml;manları bir arada tutmak i&amp;ccedil;in kullanılırlar ve ayrıca erişim kontrol&amp;uuml; de sağlarlar. Rust'ta mod&amp;uuml;l i&amp;ccedil;i &amp;ouml;ğelerin standart erişim seviyesi &lt;strong&gt;private&lt;/strong&gt; şeklindedir. Yani bir mod&amp;uuml;l i&amp;ccedil;indeki &amp;ouml;ğelere sadece o mod&amp;uuml;l&amp;uuml;n i&amp;ccedil;inden erişilebilir. Ancak &lt;strong&gt;pub&lt;/strong&gt; anahtar kelimesi kullanılarak bu &amp;ouml;ğelerin erişim seviyesi artırılabilir ve dışarıdan erişime a&amp;ccedil;ılabilir. Ayrıca mod&amp;uuml;ller hiyerarşik bir yapıya sahip olabilir ve alt mod&amp;uuml;ller oluşturulabilir. Bu durumda erişim kontrol&amp;uuml; daha da detaylandırılabilir. &amp;Ouml;rneğin bir mod&amp;uuml;l i&amp;ccedil;indeki bazı fonksiyonlar veya yapılar sadece o mod&amp;uuml;l i&amp;ccedil;inden erişilebilirken, bazıları dışarıdan erişime a&amp;ccedil;ık olacak şekilde tasarlanabilir. Bazı durumlarda bir veri yapısının&lt;em&gt;(struct)&lt;/em&gt; yalnızca kendi implementasyon bloğu i&amp;ccedil;indeki metotlar tarafından değiştirilmesi istenebilir. B&amp;ouml;yle bir senaryoda veri yapısını bir mod&amp;uuml;l i&amp;ccedil;ine alıp kaps&amp;uuml;lleme&lt;em&gt;(encapsulation)&lt;/em&gt; sağlanabilir. Aşağıdaki &amp;ouml;rnek kod par&amp;ccedil;asında bu durum ele alınmaktadır.&lt;/p&gt;
&lt;pre class="brush:cpp;auto-links:false;toolbar:false" contenteditable="false"&gt;fn main() {
    let mut settings = settings::AppSettings::new(settings::LogLevel::Info);
    println!("Initial Settings: {:?}", settings);

    settings.set_connections(200);
    settings.set_port(9090);
    println!("Settings after update: {:?}", settings);
}

/*
    settings mod&amp;uuml;l&amp;uuml;nde yer alan LogLevel pub erişim belirleyicisi ile tanımlanmış bir enum'dur.
    Dolayısıyla settings mod&amp;uuml;l&amp;uuml; dışından da erişilebilir.
    
    mod&amp;uuml;le veri yapılarından olan AppSettings struct'ı da dışarıdan erişilebilir &amp;ccedil;&amp;uuml;nk&amp;uuml; o da
    pub erişim belirleyicisi ile tanımlanmıştır. Ancak, AppSettings struct'ının bazı alanları
    (max_connections ve port) pub olarak tanımlanmadıklarından dışarıdan doğrudan erişilemezler.
    Bu alanlara erişim ve değiştirme işlemleri i&amp;ccedil;in public metotlar (getters ve setters) ile sağlanır.

    AppSettings veri yapısı new metodu ile oluşturulurken LogLevel değerini dışarıdan alabilir ancak,
    max_connections ve port alanları varsayılan değerlerle (DEFAULT_MAX_CONNECTIONS ve DEFAULT_PORT)
    başlatılır. Bu sayede, dışarıdan erişilemeyen alanların kontrol&amp;uuml; mod&amp;uuml;l i&amp;ccedil;inde tutulmuş olur.
    Yani, bir encapsulation (kaps&amp;uuml;lleme) sağlanmış olur.
*/
#[allow(dead_code)]
mod settings {

    #[derive(Debug)]
    pub enum LogLevel {
        Error,
        Warn,
        Info,
        Debug,
        Trace,
    }

    #[derive(Debug)]
    pub struct AppSettings {
        pub log_level: LogLevel,
        max_connections: u32,
        port: u16,
    }

    impl AppSettings {
        const DEFAULT_MAX_CONNECTIONS: u32 = 100;
        const DEFAULT_PORT: u16 = 8080;

        pub fn new(log_level: LogLevel) -&amp;gt; Self {
            AppSettings {
                log_level,
                max_connections: Self::DEFAULT_MAX_CONNECTIONS,
                port: Self::DEFAULT_PORT,
            }
        }

        pub fn get_connections(&amp;amp;self) -&amp;gt; u32 {
            self.max_connections
        }

        pub fn set_connections(&amp;amp;mut self, connections: u32) {
            self.max_connections = connections;
        }

        pub fn get_port(&amp;amp;self) -&amp;gt; u16 {
            self.port
        }

        pub fn set_port(&amp;amp;mut self, port: u16) {
            self.port = port;
        }
    }
}&lt;/pre&gt;
&lt;p&gt;&lt;img src="/image.axd?picture=/2025/Kasim/rust_exc_18.png" alt="" /&gt;&lt;/p&gt;
&lt;p&gt;B&amp;ouml;ylece geldik bir makalemizin daha sonuna.&lt;/p&gt;
&lt;p&gt;&lt;a href="https://github.com/buraksenyurt/friday-night-programmer/tree/main/src/rust-exercises" target="_blank"&gt;Bu b&amp;ouml;l&amp;uuml;mde yer alan kod par&amp;ccedil;alarına github reposu &amp;uuml;zerinden de erişebilirsiniz&lt;/a&gt;. Ayrıca &lt;a href="https://github.com/MariaLetta/free-ferris-pack" target="_blank"&gt;Ferris logosunu sevdiyseniz Maria Letta'nın reposunda daha fazlasını da bulabilirsiniz&lt;/a&gt; ;)&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Kaynaklar:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href="https://www.chiark.greenend.org.uk/~ianmdlvl/rust-polyglot/intro.html" target="_blank"&gt;Rust for the Polyglot Programmer&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://leapcell.io/blog/nine-rust-pitfalls" target="_blank"&gt;Nine Rust Pitfalls,Daniel Hayes&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://nostarch.com/rust-programming-language-2nd-edition" target="_blank"&gt;The Rust Programming Language, 2nd Edition&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://google.github.io/comprehensive-rust/comprehensive-rust.pdf" target="_blank"&gt;Welcome to Comprehensive Rust&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://refactoring.guru/design-patterns/rust" target="_blank"&gt;Design Patterns in Rust&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://doc.rust-lang.org/stable/rust-by-example/"&gt;Rust by Example&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://rust-unofficial.github.io/too-many-lists/index.html" target="_blank"&gt;Too Many Lists&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://rust-lang-nursery.github.io/rust-cookbook/" target="_blank"&gt;Rust Cookbook&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Bir başka yazıda g&amp;ouml;r&amp;uuml;ş&amp;uuml;nceye dek hepinize mutlu g&amp;uuml;nler dilerim.&lt;/p&gt;</content>
  <pubDate>2025-11-03 07:28:00</pubDate>
  <slug>rust-ile-kodlama-idmanlari-orta-seviye</slug>
  <tags>
    <tag>rust</tag>
    <tag>rust-pratikleri</tag>
    <tag>rust-programming-language</tag>
    <tag>rust-lang</tag>
  </tags>
  <categories>
    <category>Rust</category>
  </categories>
</post>