<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<post>
  <title>Bir Web API Hizmetinde Talepler ile CQ Arasında Mediator Kullanmak</title>
  <description>CQRS, nam-ı diğer Command Query Responsibility Segregation mimari deseni, veritabanına doğru yapılan yazma, güncelleme, silme gibi aksiyonlar ile veri çekme işlemlerini ayrıştırmayı önermektedir. Command tarafı ile ilişkilendirilen aksiyonlar sadece veri üzerinde işlem yapar ve geriye bir şey döndürmezler. Sorgulama(Query) tarafına ayrılan aksiyonlar da tam tersine sadece veri döndürmekten sorumludurlar(Listeleme veya tek bir öğe detayının döndürülmesi gibi) Bir Web API ve CQRS söz konusu olduğunda karşımıza birde Mediator tasarım kalıbının uygulanışı çıkar.</description>
  <content>&lt;p&gt;&lt;img src="/image.axd?picture=/2020/skynet/20/cqrs.png" alt="" align="right" /&gt;CQRS, nam-ı diğer Command Query Responsibility Segregation mimari deseni, veritabanına doğru yapılan yazma, g&amp;uuml;ncelleme, silme gibi aksiyonlar ile veri &amp;ccedil;ekme işlemlerini ayrıştırmayı &amp;ouml;nermektedir. Command tarafı ile ilişkilendirilen aksiyonlar sadece veri &amp;uuml;zerinde işlem yapar ve geriye bir şey d&amp;ouml;nd&amp;uuml;rmezler. Sorgulama&lt;em&gt;(Query)&lt;/em&gt; tarafına ayrılan aksiyonlar da tam tersine sadece veri d&amp;ouml;nd&amp;uuml;rmekten sorumludurlar&lt;em&gt;(Listeleme veya tek bir &amp;ouml;ğe detayının d&amp;ouml;nd&amp;uuml;r&amp;uuml;lmesi gibi)&lt;/em&gt; Bir Web API ve CQRS s&amp;ouml;z konusu olduğunda karşımıza birde Mediator tasarım kalıbının uygulanışı &amp;ccedil;ıkar.&lt;/p&gt;
&lt;p&gt;Ş&amp;ouml;yle d&amp;uuml;ş&amp;uuml;nebiliriz;&amp;nbsp;Veritabanındaki kahramanların listesini &amp;ccedil;ekmek Controller tarafına gelen bir HTTP Get talebidir ve davranışsal olarak listelemeyi ifade eder. Listelemeyi ele alacak bir Handler tanımlanabilir. Listeleme ihtiyacı oluştuğunda bunun doğru Query nesnesi ile ilişkilendirilmesi sağlanmalıdır. İşte bu noktada devreye girecek Mediator, Controller &amp;uuml;zerinden doğru Handler&amp;lt;-&amp;gt;(Command/Query) ilişkisini tesis eder. Benzer şekilde yeni bir kahramanın veritabanına eklenmesi veya silinmesi CQRS'in Command kısmını ilgilendiren bir mevzudur. Yeni kahraman eklenmesini Create isimli bir tip olarak ifade edersek bu tasarım i&amp;ccedil;erisinde bir Handler ve Command ilişkisini kurabiliriz.&lt;/p&gt;
&lt;p&gt;Bu teoriyi daha kolay anlayabilmek i&amp;ccedil;in temel CRUD&lt;em&gt;(Create Read Update Delete)&lt;/em&gt; operasyonlarını i&amp;ccedil;eren bir .Net Core Web API hizmeti &amp;uuml;st&amp;uuml;nde CQRS ve Mediator k&amp;uuml;t&amp;uuml;phanesini uygulamalı olarak &amp;ccedil;alışmakta yarar var. &amp;Ouml;rnekte Handler tiplerinin tasarlanması ve Controller tarafında Command/Query ile Handler ilişkilerinin tesis edilmesi i&amp;ccedil;in MediatR isimli Nuget paketinden yararlanılması ele alınıyor.&lt;/p&gt;
&lt;h2&gt;Projenin İnşası&lt;/h2&gt;
&lt;p&gt;&amp;Ouml;rnekte kullanılan veri kaynağı &amp;ccedil;ok &amp;ouml;nemli değil. Basit olması i&amp;ccedil;in SQLite ve Entity Framework kullanmayı tercih edebiliriz. İskelet ve gerekli kurulumları aşağıdaki terminal komutlarında olduğu gibi yapılabiliriz. Web API projesinin ve gerekli sınıfların oluşturulmasını takiben SQLite tarafı i&amp;ccedil;in de bir migration işlemi uygulamaktayız.&lt;/p&gt;
&lt;pre class="brush:bash;auto-links:false;toolbar:false" contenteditable="false"&gt;dotnet new webapi -o Marvil
cd Marvil
mkdir Model

# DbContext, basit model sınıfı ve Controller
touch Model/Hero.cs Model/MarvilDbContext.cs Controllers/HeroesController.cs

# Entity Framework, SQLite ilişkisi ve migration desteği i&amp;ccedil;in gerekli nuget paketlerinin y&amp;uuml;klenmesi
dotnet add package Microsoft.EntityFrameworkCore
dotnet add package Microsoft.EntityFrameworkCore.Sqlite

# Hero.cs, MarvilDbContext.cs, appSettings, startup.cs i&amp;ccedil;erisinde gerekli hazırlıkları yaptıktan sonra migration işlemini uygulayabiliriz
dotnet ef migrations add initial
dotnet ef database update

# Mediator rol&amp;uuml;n&amp;uuml; &amp;uuml;stlenecek MediatR paketinin eklenmesi
dotnet add package MediatR.Extensions.Microsoft.DependencyInjection

# Handler klas&amp;ouml;r&amp;uuml;n&amp;uuml;n a&amp;ccedil;ılması ve ilgili sınıf dosyalarının a&amp;ccedil;ılması
mkdir Handler
touch Handler/List.cs Handler/Single.cs Handler/Create.cs Handler/Delete.cs Handler/Update.cs Handler/GreaterThan.cs&lt;/pre&gt;
&lt;p&gt;Sırasıyla uygulamamızdaki kodlarımızı yazarak devam edelim. Bir kahramanı temsil eden model nesnemiz ve DbContext t&amp;uuml;revi ile işe başlanabilir.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Hero.cs;&lt;/strong&gt;&lt;/p&gt;
&lt;pre class="brush:csharp;auto-links:false;toolbar:false" contenteditable="false"&gt;namespace Marvil.Model
{
    public class Hero
    {
        public int Id { get; set; }
        public string Name { get; set; }
        public string AlterEgo { get; set; }
        public int ForceLevel { get; set; }
    }
}&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;MarvilDbContext.cs;&lt;/strong&gt;&lt;/p&gt;
&lt;pre class="brush:csharp;auto-links:false;toolbar:false" contenteditable="false"&gt;using Microsoft.EntityFrameworkCore;

namespace Marvil.Model
{
    public class MarvilDbContext : DbContext
    {
        public MarvilDbContext(DbContextOptions options) : base(options)
        {
        }
        public DbSet&amp;lt;Hero&amp;gt; Heroes { get; set; }
    }
}&lt;/pre&gt;
&lt;p&gt;Controller g&amp;ouml;revi &amp;uuml;stlenen HereosController sınıfının inşa edilmesinden &amp;ouml;nce Command ve Query sınıflarının yazılması gerekir. Create, List, Update, Delete...&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;List.cs;&lt;/strong&gt;&lt;/p&gt;
&lt;pre class="brush:csharp;auto-links:false;toolbar:false" contenteditable="false"&gt;using MediatR;
using Microsoft.EntityFrameworkCore;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;
using Marvil.Model;

namespace Marvil.Handler
{
    /* 
        Davranışımız listeleme. List isimli sınıfla ifade edeceğiz.
        Listelemeye karşılık gelen Query ve Handler sınıflar da bu sınıfın i&amp;ccedil;erisinde tanımlanıyorlar
        IRequest sınıfı MediatR paketiyle gelmekte
    */
    public class List
    {
        // CQRS'in Query nesnesi gibi d&amp;uuml;ş&amp;uuml;nelim.
        // Hero tipinden bir liste d&amp;ouml;nmesi gerektiği ifade ediyor
        public class Query : IRequest&amp;lt;List&amp;lt;Hero&amp;gt;&amp;gt; { }

        /*
            Listeleme işini &amp;uuml;stlenen Handler sınıfı
            Uyguladığı aray&amp;uuml;ze g&amp;ouml;re ilk parametre ile hangi Query nesnesini kullanacağı 
            ikinci parametre ile de Handler'ın geriye ne d&amp;ouml;nd&amp;uuml;receği belirtiliyor
            HeroesController sınıfında bu Handler sınıfının nasıl kullanıldığına dikkat edelim
        */
        public class Handler : IRequestHandler&amp;lt;Query, List&amp;lt;Hero&amp;gt;&amp;gt;
        {
            // Entity Context nesnesi Constructor &amp;uuml;st&amp;uuml;nde enjekte ediliyor
            private MarvilDbContext _context { get; }
            public Handler(MarvilDbContext context)
            {
                _context = context;
            }
            // IRequestHandler aray&amp;uuml;z&amp;uuml;nden gelen aşağıdaki metot Entity Tarafı ile konuşan
            // ve listeyi d&amp;ouml;nd&amp;uuml;ren operasyonu &amp;uuml;stlenmekte
            public async Task&amp;lt;List&amp;lt;Hero&amp;gt;&amp;gt; Handle(Query request, CancellationToken cancellationToken)
            {
                var heroes = await _context.Heroes.ToListAsync();
                return heroes;
            }
        }
    }
}&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Single.cs;&lt;/strong&gt;&lt;/p&gt;
&lt;pre class="brush:csharp;auto-links:false;toolbar:false" contenteditable="false"&gt;using MediatR;
using System;
using Microsoft.EntityFrameworkCore;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;
using Marvil.Model;

namespace Marvil.Handler
{
    /* 
        Genel pratik olarak ID bazlı arama yaptırıp tek bir kahraman bilgisini geri d&amp;ouml;nd&amp;uuml;ren davranışı programlıyoruz.
    */
    public class Single
    {
        /*
            CQRS'in Query nesnesi gibi d&amp;uuml;ş&amp;uuml;nelim.
            Hero tipinden bir nesne d&amp;ouml;nd&amp;uuml;receği belirtiliyor
            İsme g&amp;ouml;re arama yapacağımız i&amp;ccedil;in Name isimli bir &amp;ouml;zellik de var.
        */
        public class Query : IRequest&amp;lt;Hero&amp;gt;
        {
            public string Name { get; set; }
        }

        public class Handler : IRequestHandler&amp;lt;Query, Hero&amp;gt;
        {
            private MarvilDbContext _context { get; }
            public Handler(MarvilDbContext context)
            {
                _context = context;
            }
            public async Task&amp;lt;Hero&amp;gt; Handle(Query request, CancellationToken cancellationToken)
            {
                var hero = await _context.Heroes.FirstOrDefaultAsync(h =&amp;gt; h.Name == request.Name);
                if (hero == null)
                    throw new Exception("Aranan kahraman bulunamadı");
                return hero;
            }
        }
    }
}&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Create.cs;&lt;/strong&gt;&lt;/p&gt;
&lt;pre class="brush:csharp;auto-links:false;toolbar:false" contenteditable="false"&gt;using System;
using MediatR;
using Microsoft.EntityFrameworkCore;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;
using Marvil.Model;

namespace Marvil.Handler
{
    /*
    Yeni veri ekleme bizim i&amp;ccedil;in Create isimli bir davranış
    */
    public class Create
    {
        /*
            Command sınıfı MediatR'deki IRequest aray&amp;uuml;z&amp;uuml;nden t&amp;uuml;rer.
            CQRS'in Command nesneleri bilindiği &amp;uuml;zere geriye bir şey d&amp;ouml;nd&amp;uuml;rmeyen aksiyonlarda ele alınır. Veri ekleme gibi.
            Bu nedenle List ve Single sınıflarındaki Query tiplerinde olduğu gibi t&amp;uuml;rl&amp;uuml; bir IRequest s&amp;ouml;z konusu değildir.
            Hero sınıfı ile aynı &amp;ouml;zelliklere sahiptir.
            Handler ile ilişkilendiriecek Command nesnesidir.
        */
        public class Command : IRequest
        {
            public string Name { get; set; }
            public string AlterEgo { get; set; }
            public int ForceLevel { get; set; }
        }

        /*
            Handler sınıfı IRequestHandler&amp;lt;Command&amp;gt; aray&amp;uuml;z&amp;uuml;n&amp;uuml; uygulamakta
        */
        public class Handler : IRequestHandler&amp;lt;Command&amp;gt;
        {
            // DbContext'in enjekte edilmesi
            private readonly MarvilDbContext _context;
            public Handler(MarvilDbContext context)
            {
                _context = context;
            }

            /*
                Yeni kahraman ekleme işini ele alan metodumuz.
                İlk parametre aynı zamanda gelen talepteki bilgileri alıp yeni Hero nesnesinin &amp;ouml;rneklenmesin kullanılıyor
            */
            public async Task&amp;lt;Unit&amp;gt; Handle(Command request, CancellationToken cancellationToken)
            {
                //TODO: Kahraman daha &amp;ouml;nceden eklenmişse tekrar eklenmesin

                var hero = new Hero
                {
                    Name = request.Name,
                    AlterEgo = request.AlterEgo,
                    ForceLevel = request.ForceLevel
                };
                // DbContext &amp;uuml;st&amp;uuml;ndeki Heroes koleksiyonuna ekleniyor
                _context.Heroes.Add(hero);
                // Kayıt işlemi başarılıysa 
                var success = await _context.SaveChangesAsync() &amp;gt; 0;
                if (success)
                {
                    return Unit.Value;
                }
                else // Değilse
                {
                    throw new Exception("Kahraman listeye eklenemedi");
                }
            }
        }
    }
}&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Delete.cs;&lt;/strong&gt;&lt;/p&gt;
&lt;pre class="brush:csharp;auto-links:false;toolbar:false" contenteditable="false"&gt;using System;
using MediatR;
using Microsoft.EntityFrameworkCore;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;
using Marvil.Model;

namespace Marvil.Handler
{
    /*
        Operasyon silme.
        Create operasyonu gibi geriye veri d&amp;ouml;nd&amp;uuml;rmez.
        Command ve Handler buna g&amp;ouml;re tasarlanır.
    */
    public class Delete
    {

        // Silinmek istenen verinin Name bilgisi yeterli. Command sınıfını buna g&amp;ouml;re tasarlıyoruz.
        // Geriye veri d&amp;ouml;nd&amp;uuml;rmediğinden generic olmayan IRequest'i kullandık
        public class Command : IRequest
        {
            public string Name { get; set; }
        }

        // Silme operasyonunu ele alan Handler sınıfı
        public class Handler : IRequestHandler&amp;lt;Command&amp;gt;
        {
            private readonly MarvilDbContext _context;

            public Handler(MarvilDbContext context)
            {
                _context = context;
            }

            public async Task&amp;lt;Unit&amp;gt; Handle(Command request, CancellationToken cancellationToken)
            {
                // &amp;Ouml;nce kahramanı bulalaım
                var hero = await _context.Heroes.FirstOrDefaultAsync(h =&amp;gt; h.Name == request.Name);
                if (hero == null) //Yoksa exception fırlatıyoruz
                {
                    throw new Exception("Bu isme sahip bir kahraman listede yok");
                }
                _context.Remove(hero); // Bulduysak siliyoruz
                var success = await _context.SaveChangesAsync() &amp;gt; 0; //Unit tipinden bir şey d&amp;ouml;nd&amp;uuml;rmemiz lazım
                if (success)
                {
                    return Unit.Value;
                }
                throw new Exception("Silme işlemi sırasında bilinmeyen hata.");
            }
        }
    }
}&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Update.cs;&lt;/strong&gt;&lt;/p&gt;
&lt;pre class="brush:csharp;auto-links:false;toolbar:false" contenteditable="false"&gt;using System;
using MediatR;
using Microsoft.EntityFrameworkCore;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;
using Marvil.Model;

namespace Marvil.Handler
{
    /*
        G&amp;uuml;ncelleme tasarlandığı kısım.
    */
    public class Update
    {
        /*
            Kahraman verisi g&amp;uuml;ncellenirken kuvvetle muhtemel t&amp;uuml;m &amp;ouml;zelliklerinin son hallerini almak lazım.
            G&amp;uuml;ncelleme CQRS'in Command kısmına ait bir konu olduğundan geriye bir şey d&amp;ouml;nd&amp;uuml;rmeyeceğiz.
            Bu nedenle sadece IRequest t&amp;uuml;retmesi s&amp;ouml;z konusu.
        */
        public class Command : IRequest
        {
            public int Id { get; set; }
            public string Name { get; set; }
            public string AlterEgo{get;set;}
            public int ForceLevel { get; set; }
        }

        // G&amp;uuml;ncelleme işini &amp;uuml;stlenen Handler
        public class Handler : IRequestHandler&amp;lt;Command&amp;gt;
        {
            private readonly MarvilDbContext _context;

            public Handler(MarvilDbContext context)
            {
                _context = context;
            }

            public async Task&amp;lt;Unit&amp;gt; Handle(Command request, CancellationToken cancellationToken)
            {
                // &amp;Ouml;nce kahramanı bulalaım
                var hero = await _context.Heroes.FirstOrDefaultAsync(h =&amp;gt; h.Id == request.Id);
                if (hero == null) //Yoksa exception fırlatıyoruz
                {
                    throw new Exception("Bu isme sahip bir kahraman listede yok");
                }
                // varsa g&amp;uuml;ncelleme yapıp kaydediyoruz.
                hero.Name=request.Name;
                hero.AlterEgo=request.AlterEgo;
                hero.ForceLevel=request.ForceLevel;

                var success = await _context.SaveChangesAsync() &amp;gt; 0; //Unit tipinden bir şey d&amp;ouml;nd&amp;uuml;rmemiz lazım
                if (success)
                {
                    return Unit.Value;
                }
                throw new Exception("Silme işlemi sırasında bilinmeyen hata.");
            }
        }
    }
}&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;GreaterThan.cs;&lt;/strong&gt;&lt;/p&gt;
&lt;pre class="brush:csharp;auto-links:false;toolbar:false" contenteditable="false"&gt;using MediatR;
using System.Linq;
using Microsoft.EntityFrameworkCore;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;
using Marvil.Model;

namespace Marvil.Handler
{
    /* 
        Davranışımız yine listeleme ama biraz daha farklı.
        Bu sefer g&amp;uuml;c&amp;uuml; belli bir değerin &amp;uuml;st&amp;uuml;nde olan kahramanları d&amp;ouml;nd&amp;uuml;r&amp;uuml;yoruz
    */
    public class GreaterThan
    {
        /*
            CQRS'in Query nesnesi gibi d&amp;uuml;ş&amp;uuml;nelim.
            Hero tipinden bir liste d&amp;ouml;nmesi gerektiği ifade ediyor.
            Listenin arama kriterini de property olarak belirliyoruz.
        */
        public class Query : IRequest&amp;lt;List&amp;lt;Hero&amp;gt;&amp;gt;
        {
            public int LevelValue { get; set; }
        }

        public class Handler : IRequestHandler&amp;lt;Query, List&amp;lt;Hero&amp;gt;&amp;gt;
        {
            private MarvilDbContext _context { get; }
            public Handler(MarvilDbContext context)
            {
                _context = context;
            }
            // IRequestHandler aray&amp;uuml;z&amp;uuml;nden gelen aşağıdaki metot Entity Tarafı ile konuşan
            // ve listeyi d&amp;ouml;nd&amp;uuml;ren operasyonu &amp;uuml;stlenmekte
            public async Task&amp;lt;List&amp;lt;Hero&amp;gt;&amp;gt; Handle(Query request, CancellationToken cancellationToken)
            {
                var heroes = await _context.Heroes.Where(h =&amp;gt; h.ForceLevel &amp;gt;= request.LevelValue).ToListAsync();
                return heroes;
            }
        }
    }
}&lt;/pre&gt;
&lt;p&gt;Sonrasında bu nesneleri MediatoR yardımıyla ilişkilendiren Controller'ı yazabiliriz.&lt;/p&gt;
&lt;pre class="brush:csharp;auto-links:false;toolbar:false" contenteditable="false"&gt;using System.Collections.Generic;
using System.Threading.Tasks;
using Marvil.Model;
using MediatR;
using Microsoft.AspNetCore.Mvc;
using Marvil.Handler;

namespace API.Controllers
{
    [Route("api/[controller]")]
    [ApiController]
    public class HeroesController : ControllerBase
    {
        // Mediator nesnesini constructor &amp;uuml;zerinden enjekte ediyoruz
        private readonly IMediator _mediator;
        public HeroesController(IMediator mediator)
        {
            _mediator = mediator;
        }

        /*
            Yeni kahraman ekleme işinin ele alındığı metot. 
            Tipik HTTP Post.
            Parametre olarak Create tipi i&amp;ccedil;indeki Command sınıfı (Handler g&amp;ouml;revini &amp;uuml;stlenen) kullanılıyor
        */
        [HttpPost]
        public async Task&amp;lt;ActionResult&amp;lt;Unit&amp;gt;&amp;gt; Create(Create.Command command)
        {
            // Mediator gelen talebi uygun Handler'a y&amp;ouml;nlendirmekte
            return await _mediator.Send(command);
        }

        [HttpDelete("{name}")]
        public async Task&amp;lt;ActionResult&amp;lt;Unit&amp;gt;&amp;gt; Delete(string name)
        {
            return await _mediator.Send(new Delete.Command { Name = name });
        }

        // Veri g&amp;uuml;ncelleme işini &amp;uuml;stlenen operasyon
        [HttpPut]
        public async Task&amp;lt;ActionResult&amp;lt;Unit&amp;gt;&amp;gt; Update(Update.Command command)
        {
            return await _mediator.Send(command);
        }

        // Listeleme i&amp;ccedil;in gelen Http Get talebine karşılık &amp;ccedil;alışacak
        // Geriye Hero listesi d&amp;ouml;nd&amp;uuml;r&amp;uuml;r
        [HttpGet]
        public async Task&amp;lt;ActionResult&amp;lt;List&amp;lt;Hero&amp;gt;&amp;gt;&amp;gt; List()
        {
            // İşte en g&amp;uuml;zel kısım :)
            // Listeleme davranışı i&amp;ccedil;in devreye giren Mediator nesnesi
            // Bu isteği List Handler i&amp;ccedil;erisindeki Query sınıfına y&amp;ouml;nlendiriyor.
            // Diğer metotlarda da sadece Send fonksiyonunu &amp;ccedil;ağırdığımıza ve gerekli Query ya da Command nesnesini parametre olarak verdiğimize dikkat edelim.
            return await _mediator.Send(new List.Query());
        }

        // İsimden kahraman detaylarını d&amp;ouml;nd&amp;uuml;ren HTTP Get operasyonu
        [HttpGet("{name}")]
        public async Task&amp;lt;ActionResult&amp;lt;Hero&amp;gt;&amp;gt; Single(string name)
        {
            /*
                Single sınıfı i&amp;ccedil;indeki Query nesnesini &amp;ouml;rneklerken,
                gerekli isim parametresini de besliyoruz
            */
            return await _mediator.Send(new Single.Query() { Name = name });
        }

        [HttpGet("gt/{value}")]
        public async Task&amp;lt;ActionResult&amp;lt;List&amp;lt;Hero&amp;gt;&amp;gt;&amp;gt; GreaterThan(int value)
        {
            return await _mediator.Send(new GreaterThan.Query() { LevelValue = value });
        }
    }
}&lt;/pre&gt;
&lt;p&gt;Startup.cs tarafında Entity Framework ve MediatR i&amp;ccedil;in gerekli Middelware bildirimlerini de ConfigureServices metodu i&amp;ccedil;erisinde aşağıdaki gibi yapabiliriz.&lt;/p&gt;
&lt;pre class="brush:csharp;auto-links:false;toolbar:false" contenteditable="false"&gt;public void ConfigureServices(IServiceCollection services)
{
	// DbContext nesnesini middelware'e ekliyoruz ve appsettings dosyasındaki bağlantı bilgisi doğrultusunda Sqlite kullanacak şekilde ayağa kaldırıyoruz.
	services.AddDbContext&amp;lt;MarvilDbContext&amp;gt;(opt =&amp;gt;
	{
		opt.UseSqlite(Configuration.GetConnectionString("MarvilDbConnection"));
	});

	// Mediator nesnesini servis olarak &amp;ccedil;alışma zamanına ekliyoruz            
	services.AddMediatR(typeof(List.Handler).Assembly);

	services.AddControllers();
}&lt;/pre&gt;
&lt;h2&gt;&amp;Ccedil;alışma Zamanı&lt;/h2&gt;
&lt;p&gt;Piuvvv!!! Epey zahmetli bir yoldan ge&amp;ccedil;erek buraya kadar gelmiş olmalısınız. &amp;Ouml;yleyse &amp;ccedil;alışma zamanına ge&amp;ccedil;ebiliriz. Web API'yi &amp;ccedil;alıştırdıktan sonra Postman veya muadili bir ara&amp;ccedil;la gerekli testler yapılabilir.&lt;/p&gt;
&lt;pre class="brush:bash;auto-links:false;toolbar:false" contenteditable="false"&gt;dotnet watch run&lt;/pre&gt;
&lt;p&gt;&amp;Ouml;rnek sorgularımızı ve beklenen &amp;ccedil;ıktıları aşağıda bulabilirsiniz.&lt;/p&gt;
&lt;pre class="brush:plain;auto-links:false;toolbar:false" contenteditable="false"&gt;#Yeni Kahraman Ekleme
HTTP Post
http://localhost:5000/api/heroes

{
"Name": "Batman",
"AlterEgo": "Bruce Wayne",
"ForceLevel": 76
}&lt;/pre&gt;
&lt;p&gt;&lt;img src="/image.axd?picture=/2020/skynet/20/Screenshot_1.png" alt="" /&gt;&lt;/p&gt;
&lt;pre class="brush:plain;auto-links:false;toolbar:false" contenteditable="false"&gt;#Tek bir kahraman detayını &amp;ccedil;ekme
HTTP Get
http://localhost:5000/api/heroes/Wonder Woman&lt;/pre&gt;
&lt;p&gt;&lt;img src="/image.axd?picture=/2020/skynet/20/Screenshot_3.png" alt="" /&gt;&lt;/p&gt;
&lt;pre class="brush:plain;auto-links:false;toolbar:false" contenteditable="false"&gt;#T&amp;uuml;m kahramanların listesini &amp;ccedil;ekme
HTTP Get
http://localhost:5000/api/heroes&lt;/pre&gt;
&lt;p&gt;&lt;img src="/image.axd?picture=/2020/skynet/20/Screenshot_2.png" alt="" /&gt;&lt;/p&gt;
&lt;pre class="brush:plain;auto-links:false;toolbar:false" contenteditable="false"&gt;#G&amp;uuml;c&amp;uuml; 90nın &amp;uuml;st&amp;uuml;nde olan karakterlerin &amp;ccedil;ekilmesi
HTTP Get
http://localhost:5000/api/heroes/gt/90&lt;/pre&gt;
&lt;p&gt;&lt;img src="/image.axd?picture=/2020/skynet/20/Screenshot_4.png" alt="" /&gt;&lt;/p&gt;
&lt;pre class="brush:plain;auto-links:false;toolbar:false" contenteditable="false"&gt;#Bir kahramanı veritabanından silme
HTTP Delete
http://localhost:5000/api/heroes/Black Canary&lt;/pre&gt;
&lt;p&gt;ve&lt;/p&gt;
&lt;pre class="brush:plain;auto-links:false;toolbar:false" contenteditable="false"&gt;#Bir kahramanın verisini g&amp;uuml;ncelleme
HTTP Put
http://localhost:5000/api/heroes
{
"Id":1,
"Name": "Batman",
"AlterEgo": "Bruce Wayne",
"ForceLevel": 82
}&lt;/pre&gt;
&lt;p&gt;&lt;img src="/image.axd?picture=/2020/skynet/20/Screenshot_5.png" alt="" /&gt;&lt;/p&gt;
&lt;p&gt;Tavsiyem Controller, Handler ve MediatR ilişkisini daha iyi kavrayabilmek i&amp;ccedil;in uygulamayı debug ederek analiz etmeniz. Peki sizce CQRS desenini Mediator ile bir arada kullanmanın avantajları neler olabilir? Bu sorunun cevabını d&amp;uuml;ş&amp;uuml;n&amp;uuml;rken uygulamaya birka&amp;ccedil; ilave daha yapabilirsiniz. &amp;Ouml;rneğin kahramanların katıldığı g&amp;ouml;revleri tutan Mission isimli bir sınıf tasarlayıp Hero ile Mission arasında &amp;ccedil;oğa &amp;ccedil;ok&lt;em&gt;(karmaşık gelirse bire &amp;ccedil;ok da olur)&lt;/em&gt; ilişki tesis edip bir kahraman ve katıldığı g&amp;ouml;revler listesini kontrol edecek Handler-Query tiplerini entegre etmeye &amp;ccedil;alışabilirsiniz. Buna ek olarak kahramanın g&amp;uuml;c&amp;uuml;n&amp;uuml;n belli bir değerden k&amp;uuml;&amp;ccedil;&amp;uuml;k olması i&amp;ccedil;in ayrı bir Handler&lt;em&gt;(LessThan isimli)&lt;/em&gt; yazmak yerine GreaterThan ve Equal gibi operasyonları da i&amp;ccedil;erisine alacak ortak bir Handler tasarımı yapmayı d&amp;uuml;ş&amp;uuml;nebilirsiniz.&lt;/p&gt;
&lt;p&gt;B&amp;ouml;ylece geldik bir &lt;a href="https://github.com/buraksenyurt/skynet" target="_blank"&gt;SkyNet&lt;/a&gt; derlemesinin daha sonuna. &amp;Ouml;rneğin t&amp;uuml;m kodlarına &lt;a href="https://github.com/buraksenyurt/skynet/tree/master/No%2020%20-%20CQRS%20with%20Mediator" target="_blank"&gt;github reposundan&lt;/a&gt; erişebilirsiniz. Tekrardan g&amp;ouml;r&amp;uuml;ş&amp;uuml;nceye dek hepinize mutlu g&amp;uuml;nler dilerim.&lt;/p&gt;</content>
  <pubDate>2020-05-29 17:23:00</pubDate>
  <slug>bir-web-api-hizmetinde-request-ler-ile-cq-arasinda-mediator-kullanmak</slug>
  <tags>
    <tag>cqrs</tag>
    <tag>c#</tag>
    <tag>entity-framework</tag>
    <tag>rest</tag>
    <tag>rest-api</tag>
    <tag>rest-based-services</tag>
    <tag>mediatr</tag>
    <tag>mediator</tag>
  </tags>
  <categories>
    <category>.Net Core</category>
  </categories>
</post>